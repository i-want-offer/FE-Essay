# 深拷贝

```typescript
function deepClone<T extends object | Array>(val: T): T{
  const initial = Array.isArray(val)? [] : {}
  for(const key in val) {
    if(typeof val[key] === 'object')
      initial[key] = deepClone(val[key])
    else initial[key] = val[key]
  }
  return initial
}
```

